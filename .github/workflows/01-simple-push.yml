name: "01 - simple exemple"

on: push

jobs:
  afficher-info:
    runs-on: ubuntu-latest
    steps:
      - name: "afficher des infos"
        run: |
          echo "push detected"
          echo "author : ${{ github.actor }}"
          echo "author : ${{ github.ref_name }}"
          echo "author : ${{ github.sha }}"